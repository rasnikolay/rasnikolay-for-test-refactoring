function calculateAdd(a: number, b:number): number {
  return a + b;
}

/* Плохо: название function expression и отсуствует аннотация типов

  Улучшено: более понятное описание выполняемой функции
 */