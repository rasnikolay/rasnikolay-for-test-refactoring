const rollMany = (n: number, pins: number) => {
  return console.log(Array(n).fill(pins));
};

/* По сути эта работа метода fill, для этого нам нужен Array и в него передать количество значений из N*/