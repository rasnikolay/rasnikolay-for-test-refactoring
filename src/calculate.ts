const calculateSquare = (number: number): number => {
  return number * number;
};

/* Плохо: название function expression и отсутствие ожидаемого Типа данных на выходе

   Улучшено: более понятное описание выполняемой функции
 */
